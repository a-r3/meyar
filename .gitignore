__pycache__/
*.pyc
.venv/
.env
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
node_modules/
.DS_Store
*.bak-*

# Never commit real candidate data — synthetic fixtures only.
real_cvs/
customer_data/
prod_data/

# Local document storage (uploaded CV bytes) — never committed.
backend/var/

# Local owner acceptance evidence packs (screenshots of the demo UI) —
# never committed; delivered to the owner as local files only.
local_evidence/

.claude/scheduled_tasks.lock
